/*
Local scope

1. Variables defined inside a FUNCTION cannot be used outside the function
2. Variables defined inside a IF ELSE BLOCK cannot be used outside the IF ELSE BLOCK
3. Variables defined inside a LOOPS cannot be used outside the LOOPS
3. Variables defined inside a "{ }" cannot be used outside the "{ }"
*/